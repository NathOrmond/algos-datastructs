import { ExampleType } from "./types";
/**
 * This file contains stubs for the algorithm
 * in order to test or experiment with writing
 * a solution.
 * @param param 
 * @returns param
 */
export const algorithm = (param: ExampleType): ExampleType => {
    // This will fail tests but has correct return type
    const wrongReturn: ExampleType = { exampleProperty: -Infinity };

    // TODO 

    return wrongReturn;
}