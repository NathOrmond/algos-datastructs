import { ExampleType } from "./types";
/**
 * This file contains an implementation 
 * of a correct solution that passes all tests
 * @param param 
 * @returns param
 */
export const algorithm = (param: ExampleType): ExampleType => {
    return param;
}